using Microsoft.Xna.Framework;
using ProjectVivid7.ECS;
using System;
using System.Collections.Generic;
using System.Text;

namespace ProjectVivid7.Utilities.Extensions
{
    /*public static class GameColorExtensions
    {
        // Convert GameColors to Microsoft.Xna.Framework.Color 
        public static Dictionary<GameColors, Color> colorValues = new Dictionary<GameColors, Color>()
        {
            { GameColors.None, Color.Black },
            { GameColors.Red, Color.Red },
            { GameColors.Yellow, Color.Gold },
            { GameColors.Blue, Color.Blue },
            { GameColors.Orange, Color.DarkOrange },
            { GameColors.Green, Color.Green },
            { GameColors.Purple, Color.Purple },
            { GameColors.All, Color.White }
        };

        public static Color ToColor(this GameColors pColor)
        {
            return colorValues[pColor];
        }

        // Check if a GameColors contains another GameColors
        public static bool ContainsColor(this GameColors pC1, GameColors pC2)
        {
            return (pC1 & pC2) == pC2;
        }

        // Alter GameColors based on the color wheel
        public static GameColors Blend(GameColors pC1, GameColors pC2)
        {
            return pC1 | pC2;
        }
        public static GameColors BlendWith(this GameColors pC1, GameColors pC2)
        {
            return pC1 | pC2;
        }
        public static GameColors GetComplementaryColor(this GameColors pColor)
        {
            return ~pColor;
        }
    }*/
}
